# Redis Headless Service for StatefulSet DNS discovery
apiVersion: v1
kind: Service
metadata:
  name: redis-headless
  namespace: microshop
spec:
  clusterIP: None  # Headless service
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
